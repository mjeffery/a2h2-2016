## A2 Health Hacks!

To get going:

```
$ cordova platform add browser
$ npm install -g bower
$ bower install
$ cordova plugin add cordova-plugin-browsersync
```

To run (live reload):

```
$ cordova run -- --live-reload
```
